# ZHBK1 app
Build:
```
git pull                # sync changes
cd collections/zhbk1    # go to directory

# build
./build.bat             # on windows
./build.sh              # on unix (macos, linux)

# run
./bin/zhbk1.exe         # on windows
./bin/zhbk1             # on unix (macos, linux)
```

